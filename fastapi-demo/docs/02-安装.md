# 创建虚拟环境
python -m venv .venv

# 激活虚拟环境
# Linux/macOS
source .venv/bin/activate

# 安装依赖
```shell
./.venv/bin/python3 -m pip install "fastapi[standard]" uvicorn
./.venv/bin/python3 -m pip install sqlalchemy aiomysql
```

## 启动项目
```shell
fastapi dev
```